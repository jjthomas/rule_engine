#!/bin/bash

AGFI=agfi-0d30635ce2192c28f

if [[ $(sudo fpga-describe-local-image -S 0 -H) != *"$AGFI"* ]]; then
  echo "Loading AFI..."
  sudo fpga-load-local-image -S 0 -I $AGFI > /dev/null
fi

args="${@:2}"
sudo $(which python3) $1 $args
